# Maria AdminOS AI

Local-first business administration platform (Steps 1–18 production build).

Data is stored in the browser (localStorage) — no backend required.

## Run locally
```bash
npx serve .
# or
python3 -m http.server 8080
```
